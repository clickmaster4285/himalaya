import type { Villa } from "@/lib/villa-types";
import { CATALOG_VILLAS } from "@/lib/villa-catalog";

/** Seed + fallback reference — matches homepage villa catalog. */
export const STATIC_VILLAS: Villa[] = CATALOG_VILLAS;
